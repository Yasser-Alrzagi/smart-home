# D8 — نتائج التسليم: لوحات الضباط

| البند | الحالة |
|---|---|
| service قراءة فقط لكل دور | ✅ `app/services/dashboards.py` — 9 ملفات بناء حسب الدور |
| عقد موحد | ✅ `stats` + `attention` + `latest` + `actions` |
| نقطة النهاية | ✅ `GET /api/v1/dashboards/me` (مثبتة في `main.py`) |
| الواجهة | ✅ لوحة قيادة لغير الطالب؛ رحلة الطلب باقية للطالب |
| اختبارات تكامل D8 | ✅ 5 سيناريوهات (بما فيها 9 أدوار) |
| المجموعة الكاملة | بانتظار التشغيل الكامل |
| lint | ✅ `ruff` + `node --check` |
| فحص حي | بانتظار إعادة البذر |

## ملاحظات
- لا مهاجرة، ولا عمود جديد — D8 لا يغيّر البنية إطلاقاً.
- «غائبون اليوم» يربط لوحة إدارة السكن بمخرجات D7 مباشرة.
- لا يلمس بوابة الطالب (رحلته قائمة بذاتها).

## ما تبقى من النطاق المتبقي
1. ✅ الإشعارات (D6)
2. ✅ الغياب غير المصرح / سجل الحضور اليومي (D7)
3. ✅ لوحات الضباط (D8)
4. ⏳ تنظيف AI (BFS/A*) (D9) — الأخير
