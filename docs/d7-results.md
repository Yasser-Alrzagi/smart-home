# D7 — نتائج التسليم: سجل الحضور اليومي والغياب غير المصرح

| البند | الحالة |
|---|---|
| نموذج + مهاجرة `attendance_records` | ✅ `d7a1b2c3d4e5` فوق `d3b7a21c8f04`؛ `SCHEMA_HEAD` محدّث؛ enum `AttendanceStatus` |
| API (تسجيل دفعة، سجل يوم، سجل الطالب، قائمة اختيار) | ✅ مثبّتة في `main.py` |
| قاعدة الغياب غير المصرح | ✅ توليد `unauthorized` مرة واحدة بمرجع `attendance:<id>`؛ لا حذف للأثر عند التصحيح |
| التغطية | ✅ إذن معتمد أو خروج طارئ موثق يمنع التوليد |
| الصلاحيات | ✅ إدارة السكن تكتب؛ الطالب يقرأ سجله فقط؛ الباقي 403 |
| Audit | ✅ `attendance.record` بتفاصيل معرفات فقط |
| الواجهة | ✅ صفحة «الحضور اليومي» لإدارة السكن + سجل الطالب الشخصي |
| اختبارات تكامل D7 | ✅ 4 سيناريوهات HTTP/DB |
| **المجموعة الكاملة** | ✅ **499 مرت، 3 مخطئة** (كانت 488) — 78 ثانية |
| lint | ✅ `ruff check .` نظيف — ✅ `node --check portal.js` |
| فحص حي (منفذ 9011) | ✅ |
| الالتزام المحلي | بحسب هذه الدفعة |
| الدفع إلى GitHub | بانتظار المستخدم (لا يوجد remote في هذه البيئة) |

## ملاحظات

- عقد الاختبارات المحدثة مع البنية: `EXPECTED_TABLES` (27 → 28)، قيد فريد، `owned children CASCADE`، `actor SET NULL`، سلسلة Alembic، ومستند `docs/database.md` (الجدول + `attendance_status` + العلاقات).
- لم تُغيّر أي سياسة معتمدة: الإذونات والبلاغات الطارئة (D5) تبقى مصدر التوثيق الفوري؛ سجل الحضور اليومي يوثّق الواقع اليومي.

## ما تبقى من النطاق المتبقي

1. ✅ الإشعارات (D6) — مسلّمة ومقفلة
2. ✅ الغياب غير المصرح / سجل الحضور اليومي (D7) — هذه الدفعة
3. ⏳ لوحات معلومات الضباط (D8)
4. ⏳ تنظيف AI (BFS/A*) (D9)
