# تسليم D6 — الإشعارات

**الحالة: نُفِّذت واختُبرت محلياً في 7 سبتمبر 2026. لم تُرفع إلى GitHub بعد.**

الأساس `f95617e` (د5 المرفوعة)، والعمل فوقها في فرع `feat/notifications-d6`.

## ما أصبح متاحاً؟

1. **صندوق إشعارات لكل مستخدم** عبر البوابة العربية: بند «الإشعارات» مع عدّاد غير المقروء، وزرا «تعليم كمقروء» و«تعليم الكل كمقروء».
2. **إطلاق تلقائي من أحداث د5** (داخل معاملة الحدث نفسها): تسجيل/إلغاء خدمة، شكوى جديدة وتحديثها، طلب صيانة جديد وتحديثه، إذن غياب وقراره، بلاغ طارئ وتوثيقه.
3. **ملكية صارمة**: قراءة إشعار الغير 404 دون كشف وجوده؛ لا إنشاء/تعديل/حذف من العميل إطلاقاً.
4. **تدقيق**: `notification.read` و`notification.read_all` بمعرّفات وأرقام فقط.

## التحقق الفعلي

| الفحص | النتيجة |
|---|---|
| `pytest -q` (كامل المستودع) | **488 ناجحة، 3 متخطّاة** |
| اختبارات د6 (4 سيناريوهات تكامل HTTP/DB) | **4 ناجحة** |
| `ruff check .` | **All checks passed** |
| `node --check app/web/static/portal.js` | سليمة |

## المتبقي من خطة المشروع

- الغياب غير المصرّح وسجل الحضور اليومي، ولوحات الضباط، وذكاء التنظيف (BFS/A*).
- واجبات تشغيلية موثقة منذ د2: TLS/بروكسي، نسخ احتياطي، MFA، مدة بقاء التدقيق (بقرار).

## حالة الرفع

- فرع `feat/notifications-d6` محلياً؛ الشجرة نظيفة. الرفع عبر حزمة (bundle) كما في د5.
