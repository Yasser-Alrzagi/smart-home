[pytest]
testpaths = tests
pythonpath = .
addopts = --strict-markers
markers =
    db: requires an explicitly configured, isolated TEST_DATABASE_URL
    migration_cycle: destructive DDL only on the separate opt-in test migration database
